<h1>Listing users</h1>

<table>
  <tr>
    <th>id</th>
    <th>Nom</th>
    <th>Admin</th>
    <th>Dirección</th>
    <th>Telefono</th>
    <th>Rut</th>
    <th></th>

  </tr>
  

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.name %></td>
    <td><%= user.isAdmin %></td>
    <td><%= user.address %></td>
    <td><%= user.phone %></td>
    <td><%= user.rut %></td>
		<td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?',
                                     :method => :delete %></td>
  </tr>
<% end %>
</table>

<br/>

<%= link_to 'New User Admin', new_admin_path %>